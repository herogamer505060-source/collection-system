---
description: Plan and build a full-stack feature with the project workflow
agent: build
---

Use the `fullstack-feature` skill if it applies.

Implement this request end to end:

```text
$ARGUMENTS
```

Requirements:
- inspect routes, data model, validation, and UI impact before editing
- use `supabase` MCP if the feature touches data or auth
- use `context7` for framework uncertainty and `gh_grep` for proven patterns when helpful
- run the most relevant verification commands before finishing
- report changed files, verification results, and any residual risk
