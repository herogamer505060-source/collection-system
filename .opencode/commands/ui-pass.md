---
description: Improve a page or component with a strong frontend quality pass
agent: build
---

Use the `frontend-design-pass` skill if it applies.

Review and improve this UI target:

```text
$ARGUMENTS
```

Requirements:
- preserve the existing product language while improving hierarchy, spacing, and states
- check Arabic-first readability and responsive behavior
- use `playwright` for local browser validation when the route is runnable
- summarize the visual improvements and any remaining UX debt
